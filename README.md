# OpenBook
Notion-style organizer / study / notes app

## Tech stack

This project is based on React using Vite for builds, the database is SQL. Cluster is used to seperate a web and database backend.